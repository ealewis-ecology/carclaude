# Pinned to compatible releases (~=) so a fresh install can't silently pull a
# breaking minor of a pre-1.0 dependency. For stricter, tamper-evident installs,
# generate a hashed lockfile:  pip-compile --generate-hashes -o requirements.lock
# (or `uv lock`) and install with  pip install --require-hashes -r requirements.lock
fastapi~=0.136.3
uvicorn[standard]~=0.49.0
httpx~=0.28.1
python-multipart~=0.0.32
python-dotenv~=1.2.2
claude-agent-sdk~=0.2.101
